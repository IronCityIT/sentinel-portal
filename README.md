# ICIT Sentinel Portal - Complete File Structure

## Upload ALL of these files to your GitHub repo in this exact structure:

```
sentinel-portal/
│
├── app/
│   ├── page.tsx                 ← Main landing page
│   ├── layout.tsx               ← Root layout with Auth0
│   ├── globals.css              ← Global styles
│   └── dashboard/
│       └── page.tsx             ← Protected dashboard
│
├── public/
│   ├── icit-sentinel-logo.svg           ← Main logo
│   ├── icit-sentinel-logo-compact.svg   ← Compact logo
│   └── icit-sentinel-icon.svg           ← Favicon
│
├── package.json                 ← Dependencies
├── next.config.js               ← Next.js config
├── tailwind.config.js           ← Tailwind config
├── postcss.config.js            ← PostCSS config
├── tsconfig.json                ← TypeScript config
├── .env.local                   ← Environment variables (DON'T commit)
├── .gitignore                   ← Git ignore file
└── README.md                    ← This file
```

## All files are in the downloads folder - organize them like this in GitHub!
